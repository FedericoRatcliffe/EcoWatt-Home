<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.4" />
    <PackageReference Include="MQTTnet" Version="5.2.0.1603" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\EcoWattCasa.Infrastructure\EcoWattCasa.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Los perfiles de carga son internal: los tests verifican que lo que publica el mock
         sea lo mismo que sabe leer el parser, sin tener que exponerlos al resto. -->
    <InternalsVisibleTo Include="EcoWattCasa.Tests" />
  </ItemGroup>

</Project>
